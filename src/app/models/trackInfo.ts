export interface TrackInfo {
  activityName: string;
  averageSpeed: string;
  elapsedTime: number;
  id: string;
  name: string;
  pace: string;
  polyline: string;
  source: string;
  sportsTrackerUsername: string;
  startedAt: Date;
  units: number;
}
